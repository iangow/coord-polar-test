[project]
name = "coord-polar-test"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "plotnine",
    "plotnine-polars",
    "socviz-py>=0.0.5",
    "polars",
    "jupyter",
]

[tool.uv.sources]
plotnine = { path = "/Users/igow/git/plotnine", editable = true }
